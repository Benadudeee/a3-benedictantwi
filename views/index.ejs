<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta name="description" content="A simple two tiered web application where you can make a todo list">
    <title>Welcome to Simulator <%=username%></title>

    <link rel="stylesheet" type="text/css" href="css/tw.css">
</head>
<body class="p-4 sm:max-w-full md:w-4xl mx-auto bg-sky-950 text-gray-100">

    <header class="site--head ">
        <nav class="flex sm: flex-col items-center gap-y-8  md:flex-row justify-between">
            <div>
                <h2>Planner App</h2>
            </div>
            <ul class="flex justify-around gap-x-8">
                <li><a href="/">Home</a></li>
                <li><a href="/login">Login</a></li>
                <li><a href="/signup">Sign Up</a></li>
                <li><a href="/logout">Logout</a></li>
            </ul>
        </nav>
    </header>

    <main class="py-20">
        <% if (login) { %>
            <section class="mb-16">
                <div class="mb-16">
                    <h1 class="text-2xl md:text-3xl lg:text-4xl mb-4">Welcome <%=username %> </h1>
                    <p>Create a task to get started. From there, you can edit and delete the tasks you added</p>
                </div>
                
                
                    <form id="add-form" class="entryform">
                        <div class="flex flex-col mb-4" >
                            <label for="task_name">Task Name</label>
                            <input class="input input-name bg-gray-500" type="text" id="yourname">
                        </div>
                        
                        <div class="flex flex-col mb-4" >
                            <label for="task_name">Description (optional)</label>
                            <textarea class="input input-description bg-gray-500" type="text" id="yourname"></textarea>
                        </div>
                        
                        <div class="flex flex-col mb-4" >
                            <label for="task_name">Due Date</label>
                            <input class="input input-date bg-gray-500" type="date" id="yourname">
                        </div>


                        <button class="add-btn cursor-pointer px-4 py-2 bg-red-400">Add Task</button>
                    </form>
            </section>

            <section class="task-list">
                <div class="task-head mb-4">
                    <h1 class="text-2xl md:text-3xl">All Tasks</h1>
                </div>

                <div id="task-notification-bar"></div>

                <div id="task-container" class="tasks">

                </div>
            </section>
        <% } else { %>
            <div class="mb-4">
                <h1 class="text-2xl md:text-3xl lg:text-4xl ">Welcome to the </h1>
                <h1 class="text-2xl md:text-3xl lg:text-4xl"><span class="text-red-400 underline">Two Tiered</span> Planner Webapp</h1>
            </div>
    
            <p>It seems that you haven't created an account yet. An account is required to use this application</p>

        <% } %>
    </main>

    <script src="js/main.js"></script>

    <% if(login) { %>
        <script src="js/app.js"></script>
    <% } %>
</body>
</html>