<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">

    <link rel="stylesheet" type="text/css" href="css/style.css">
    <link rel="stylesheet" type="text/css" href="css/tw.css">
    
    <title>Sign Up</title>
</head>
<body class="p-4 sm:max-w-full md:w-4xl mx-auto bg-sky-950 text-gray-100">
    <header class="site--head mb-4">
        <nav class="flex sm: flex-col items-center gap-y-8  md:flex-row justify-between">
            <div>
                <h2>Planner App</h2>
            </div>
            <ul class="flex justify-around gap-x-8">
                <li><a href="/">Home</a></li>
                <li><a href="/login">Login</a></li>
                <li><a href="/signup">Sign Up</a></li>
                <li><a href="/logout">Logout</a></li>
            </ul>
        </nav>
    </header>

    <% if(status !== "") { %>
        <p class="bg-red-400 max-w-full p-1"><%=status%></p>
    <% } %>

    <main class="py-20">
            <div class="mb-8">
                <h1 class="text-2xl md:text-3xl lg:text-4xl"><span class="text-red-400 underline">Create an Account</span></h1>
                <p>Here, you can create an account.</p>
            </div>

            <form class="flex-col gap-y-md" action="/signup" method="POST">
                <div class="flex flex-col mb-4">
                    <label for="Username">Username</label>
                    <input class="bg-gray-500" name="username" type="text">
                </div>
            
                <div class="flex flex-col mb-4">
                    <label for="Password">Password</label>
                    <input class="bg-gray-500" name="password" type="password">
                </div>

                <div class="flex flex-col mb-4">
                    <label for="Password2">Password Again</label>
                    <input class="bg-gray-500" name="password2"  type="password">
                </div>
            
                <div>
                    <button type="submit" class="cursor-pointer px-4 py-2 bg-red-400">Create Account</button>
                </div>
        </form>
    </main>
</body>
</html>